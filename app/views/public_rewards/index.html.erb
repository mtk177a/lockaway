<div class="mx-auto w-full max-w-5xl">
  <h2 class="my-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900"><%= t('.heading') %></h2>

  <div class="container">
    <div class="mx-5 mb-4 flex items-center">
      <%= render 'shared/reward_search_sort', url: public_rewards_path %>
    </div>

    <turbo-frame id="reward_list">
      <%= render 'shared/reward_list', rewards: @habit_rewards %>
    </turbo-frame>

    <div class="text-center my-10">
      <%= paginate @habit_rewards %>
    </div>
  </div>
</div>
