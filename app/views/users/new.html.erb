<div class="sm:mx-auto sm:w-full sm:max-w-sm mx-5">
  <h2 class="mt-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900"><%= t('.heading') %></h2>

  <%= render "form", user: @user, button_text: t('helpers.submit.create') %>

  <!---
  <div class="sm:mx-auto sm:w-full sm:max-w-sm">
    <div class="divider text-gray-400">OR</div>
  </div>

  <%= render 'shared/google_login_button' %>
    --->
</div>
